language: swift
osx_image: xcode10.2
xcode_destination: platform=iOS Simulator,name=iPhone X
xcode_scheme: NovelReader
xcode_workspace: NovelReader.xcworkspace
script:
 #- pod repo update
 #- pod install --repo-update
 #- xcodebuild -scheme NovelReader -workspace "NovelReader.xcworkspace" -sdk iphonesimulator clean build | xcpretty -c
after_success:
  - bash <(curl -s https://codecov.io/bash)
